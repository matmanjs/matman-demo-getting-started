{
    "name": "baidu_02",
    "version": "1.0.0",
    "scripts": {
        "build": "matman build",
        "build-dev": "matman build --dev",
        "test": "npm run build && mocha"
    },
    "dependencies": {
        "matman": "^5.0.2"
    },
    "devDependencies": {
        "chai": "^4.2.0",
        "mocha": "^7.1.2"
    }
}
